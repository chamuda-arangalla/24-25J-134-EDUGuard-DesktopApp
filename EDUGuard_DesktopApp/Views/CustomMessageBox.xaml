<Window x:Class="EDUGuard_DesktopApp.Utilities.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notification"
        Height="200"
        Width="450"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStartupLocation="Manual">
    <Border Background="White" CornerRadius="5" BorderBrush="Gray" BorderThickness="1" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <!-- Content area takes available space -->
                <RowDefinition Height="*" />
                <!-- Button area takes only as much space as needed -->
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Content: Warning icon and dynamic message text -->
            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,15">
                <Image Source="/EDUGuard_DesktopApp;component/Assets/warning.png"
                       Width="32" Height="32" Margin="0,0,10,0"/>
                <TextBlock x:Name="MessageTextBlock"
                           Text="Your message here"
                           FontSize="14"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"
                           Foreground="Black"/>
            </StackPanel>

            <!-- Button: OK button aligned to the right -->
            <Button x:Name="OkButton"
                    Content="OK"
                    Width="80"
                    Height="30"
                    Grid.Row="1"
                    HorizontalAlignment="Right"
                    Click="OkButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#FFDDDDDD"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="5"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="BorderBrush" Value="Gray"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="5">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
